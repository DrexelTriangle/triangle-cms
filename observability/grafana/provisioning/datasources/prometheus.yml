apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    # Must stay "prometheus": the provisioned CMS dashboard binds 14 of its
    # panels to this UID.
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
